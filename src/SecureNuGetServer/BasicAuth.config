<?xml version="1.0"?>
<basicAuth>
  <credentials>
    <add username="username" password="password" />
  </credentials>
  <excludes>
    <!-- Allow push without basic authentication. We specify an Api key in the request instead. -->
    <add url="^/nuget" verb="put" />
  </excludes>
</basicAuth>
